---
description: (Legacy) Runs the RPV pipeline. This has been updated to route through the new adaptive orchestrator (/start-task). Usage: /new-feature "feature description"
---

You are running `/new-feature` for: "$ARGUMENTS"

Note: `/new-feature` has been updated to run via the Orchestrator to support adaptive execution, saving tokens on small tasks and maintaining rigor on large ones.

Please route this task through the adaptive workflow defined in `.claude/commands/start-task.md`. Follow the instructions in `start-task.md` exactly to orchestrate and execute this request.