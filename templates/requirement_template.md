# Feature
{feature_name}

# Business Goal
{one_sentence_business_goal}

# Actors
{list_of_actors}

# Functional Requirements
{numbered_list}

# Non-Functional Requirements
(performance, offline behavior, accessibility — include only if relevant)

# Edge Cases
{numbered_list}

# Acceptance Criteria
{numbered_list, written as testable statements}

# Open Questions
{anything ambiguous in the original request that needs human clarification
before planning proceeds — this section may be empty, but the agent must
explicitly state "None" rather than omit it}

# Refinement Findings
{appended later by the Refine-Spec Agent — leave as "Not yet refined" until
that stage runs}